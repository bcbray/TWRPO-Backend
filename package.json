{
    "name": "twrponly",
    "private": true,
    "workspaces": [
        "frontend",
        "packages/api"
    ],
    "scripts": {
        "clean": "yarn workspaces run clean",
        "build": "yarn workspaces run build",
        "heroku-postbuild": ". ./scripts/react-env.sh && cd frontend && yarn build",
        "start": "cd frontend && yarn start:server"
    }
}
